name = "cf-ai-weathering-for-you"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# Workers AI binding
[ai]
binding = "AI"

# Optional: KV namespace for caching (can be added later)
# [[kv_namespaces]]
# binding = "WEATHER_CACHE"
# id = "YOUR_KV_NAMESPACE_ID"

# Route configuration (update after deployment)
# routes = [
#   { pattern = "your-domain.com/*", zone_name = "your-domain.com" }
# ]

# For development
[env.dev]
name = "cf-ai-weathering-for-you-dev"

# For production
[env.production]
name = "cf-ai-weathering-for-you"